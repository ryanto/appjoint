{
  "name": "appjoint",
  "private": true,
  "devDependencies": {
    "@changesets/changelog-github": "^0.2.7",
    "@changesets/cli": "^2.10.3",
    "@types/react": "^16.9.43",
    "@types/react-dom": "^16.9.8",
    "cypress": "^9.5.2",
    "lerna": "^3.15.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "tsdx": "^0.14.0",
    "typescript": "^3.9.7"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna": "lerna",
    "start": "lerna run start --stream --parallel --ignore test-app-*",
    "lint": "lerna run lint -- --fix",
    "build": "lerna run build --ignore test-app-*",
    "prepublish": "lerna run prepublish",
    "changeset": "changeset",
    "release": "changeset publish",
    "version:canary": "changeset version --snapshot canary",
    "release:canary": "changeset publish --tag canary"
  }
}
